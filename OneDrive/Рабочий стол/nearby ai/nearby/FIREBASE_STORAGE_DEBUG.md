# 🐛 Отладка Firebase Storage

## ❌ Ошибка: "storage/unknown"

Эта ошибка указывает на проблему с правилами доступа или конфигурацией Storage.

## 🔧 ВРЕМЕННЫЕ ПРАВИЛА ДЛЯ ОТЛАДКИ

Замените ваши правила Storage на эти **ВРЕМЕННЫЕ** правила:

```javascript
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // ВРЕМЕННО: Разрешить все для отладки
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}

// Оставьте правила Firestore как есть
service cloud.firestore {
  match /databases/{database}/documents {
    // ... ваши существующие правила Firestore
  }
}
```

## 🧪 ТЕСТ ПОСЛЕ ИЗМЕНЕНИЯ

1. **Примените правила** в Firebase Console
2. **Откройте Developer Tools** в браузере (F12)
3. **В консоли выполните:**
   ```javascript
   window.testFirebaseStorage()
   ```
4. **Должен появиться лог:**
   ```
   🧪 Testing Firebase Storage...
   📤 Uploading test file...
   ✅ Upload successful: test-[timestamp].txt
   🔗 Getting download URL...
   ✅ Download URL obtained: https://...
   🎉 Firebase Storage test PASSED!
   ```

## 🎯 ЕСЛИ ТЕСТ ПРОЙДЕН

Попробуйте снова подать заявку с фотографией в приложении.

## 🎯 ЕСЛИ ТЕСТ НЕ ПРОЙДЕН

Проверьте:

1. **Bucket Name** в Firebase Console:
   - Storage → Settings
   - Должен быть: `nearby-74439.firebasestorage.app`

2. **Активирован ли Storage:**
   - В Firebase Console перейдите в Storage
   - Если написано "Get Started" - нажмите и активируйте

3. **Регион Storage:**
   - Должен совпадать с регионом Firestore

## 🔒 ФИНАЛЬНЫЕ ПРАВИЛА

После успешного теста замените на более безопасные:

```javascript
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Бизнес-фотографии
    match /business-photos/{fileName} {
      allow read: if true; // Публичное чтение
      allow write: if request.auth != null; // Загрузка только для авторизованных
    }
    
    // Тестовые файлы
    match /test/{fileName} {
      allow read, write: if request.auth != null;
    }
  }
}
```

**Сначала используйте временные широкие правила для отладки!** 🚀 