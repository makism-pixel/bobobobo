# 🔧 ИСПРАВЛЕНИЕ: Firebase Storage для фотографий

## 🚨 **СРОЧНО:** Исправляем ошибку "storage/unknown"

Эта ошибка возникает из-за неправильных правил безопасности Firebase Storage.

## ⚡ **БЫСТРОЕ РЕШЕНИЕ:**

### Шаг 1: Откройте Firebase Console
1. Перейдите на https://console.firebase.google.com
2. Выберите проект **nearby-74439**
3. В меню слева нажмите **"Storage"**
4. Если Storage не активирован - нажмите **"Get Started"** и активируйте
5. Перейдите на вкладку **"Правила"** (Rules)

### Шаг 2: ЗАМЕНИТЕ существующие правила

**Скопируйте и вставьте эти правила:**

```javascript
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Временно для тестирования - разрешить авторизованным пользователям
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
```

### Шаг 3: ОБЯЗАТЕЛЬНО нажмите "Опубликовать"

### Шаг 4: Протестируйте заново
1. **Закройте приложение полностью**
2. **Откройте заново**
3. **Попробуйте подать заявку с фотографией**

## 🎯 **После успешного теста - более безопасные правила:**

```javascript
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    
    // 📸 Бизнес-фотографии - основная папка
    match /business-photos/{fileName} {
      // ✅ Чтение: публичный доступ для отображения в приложении
      allow read: if true;
      
      // ✅ Загрузка: только авторизованные пользователи
      allow write: if request.auth != null;
    }
    
    // 🧪 Тестовые файлы - для отладки
    match /test/{fileName} {
      allow read, write: if request.auth != null;
    }
    
    // 🔒 Все остальные файлы - только для авторизованных
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
```

## 🔍 **Проверка что Storage активирован:**

В Firebase Console → Storage должно показывать файлы, а не кнопку "Get Started".

## ⚠️ **Если не работает:**

1. **Проверьте bucket name:** должен быть `nearby-74439.firebasestorage.app`
2. **Убедитесь что Storage активирован**
3. **Перезагрузите приложение после изменения правил**

**После настройки правил Storage ошибка исчезнет!** 🚀 