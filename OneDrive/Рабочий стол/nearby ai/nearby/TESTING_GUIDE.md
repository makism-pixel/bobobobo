# 🧪 Тестирование заявок бизнеса

## 📋 Проверка сохранения заявок в Firebase

### Способ 1: Через консоль разработчика (Быстро)

1. **Откройте приложение** в браузере/эмуляторе
2. **Откройте консоль разработчика** (F12)
3. **Выполните команду:**
   ```javascript
   createTestApplication()
   ```
4. **Проверьте консоль** - должно появиться:
   ```
   🧪 Creating test application...
   ✅ Test application created successfully
   ```

### Способ 2: Через форму регистрации бизнеса

1. **Авторизуйтесь** в приложении
2. **Перейдите на вкладку "Главная"**
3. **Нажмите "Добавить бизнес"**
4. **Заполните форму:**
   - Название: любое
   - Тип: выберите из списка
   - Телефон: обязательно
   - Адрес: обязательно
5. **Нажмите "Отправить заявку"**
6. **Проверьте консоль** - должны быть логи:
   ```
   📝 Starting application submission for user: [USER_ID] [EMAIL]
   💾 Saving business profile... {данные}
   ✅ Business profile saved successfully
   ✅ User role updated successfully
   ```

### Способ 3: Проверка в Admin-панели

1. **Войдите с почтой `malina@gmail.com`**
2. **Перейдите во вкладку "Админ"**
3. **Проверьте консоль** - должно быть:
   ```
   🔍 Admin Dashboard - User: [USER_ID] Email: malina@gmail.com isAdmin: true
   ✅ Admin access granted, loading applications...
   📄 Total applications in database: [количество]
   📋 Application: {данные заявки}
   ```

## 🔍 Диагностика проблем

### Если заявки не сохраняются:

1. **Проверьте консоль на ошибки**
2. **Убедитесь в настройках Firebase:**
   - Проект ID: `nearby-74439`
   - База данных создана
   - Правила безопасности разрешают запись

3. **Проверьте правила Firestore:**
   ```javascript
   // Временные правила для тестирования (НЕ для продакшена!)
   rules_version = '2';
   service cloud.firestore {
     match /databases/{database}/documents {
       match /{document=**} {
         allow read, write: if true;
       }
     }
   }
   ```

### Если админ-панель не показывает заявки:

1. **Проверьте email** - должен быть точно `malina@gmail.com`
2. **Проверьте консоль** на ошибки загрузки
3. **Убедитесь, что есть заявки со статусом `pending`**

## 🚀 Быстрый тест

Выполните все команды в консоли разработчика:

```javascript
// 1. Создать тестовую заявку
createTestApplication()

// 2. Проверить подключение к Firebase
console.log('Firebase config:', firebase.apps[0].options)

// 3. Проверить текущего пользователя
console.log('Current user:', firebase.auth().currentUser)
```

## 📞 Поддержка

Если проблемы не решаются:
1. Отправьте логи из консоли
2. Укажите шаги воспроизведения
3. Приложите скриншот Firebase Console 