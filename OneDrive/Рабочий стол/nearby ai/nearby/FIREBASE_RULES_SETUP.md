# 🔐 Настройка правил безопасности Firebase

## ❌ Проблема: "missing or insufficient permissions"

Эта ошибка возникает, когда правила безопасности Firebase Firestore блокируют чтение данных для незалогиненных пользователей.

## ✅ Решение: Настройка правил Firestore

### Шаг 1: Откройте Firebase Console

1. Перейдите в [Firebase Console](https://console.firebase.google.com)
2. Выберите проект **nearby-74439**
3. В левом меню нажмите **Firestore Database**
4. Перейдите на вкладку **Правила** (Rules)

### Шаг 2: Замените правила

Скопируйте и вставьте следующие правила:

```javascript
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // Правила для коллекции пользователей
    match /users/{userId} {
      // Пользователи могут читать и писать только свои собственные данные
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    
    // Правила для бизнес-профилей
    match /businessProfiles/{profileId} {
      // Чтение: авторизованные пользователи могут читать все профили
      allow read: if request.auth != null;
      
      // Создание: только владелец может создать свой профиль
      allow create: if request.auth != null && 
                   request.auth.uid == profileId &&
                   request.auth.uid == request.resource.data.userId;
      
      // Обновление: владелец может обновлять свой профиль, 
      // администраторы могут обновлять статус верификации
      allow update: if request.auth != null && (
        // Владелец может обновлять свой профиль
        (request.auth.uid == profileId) ||
        // Администратор может обновлять статус верификации
        (request.auth.token.email == 'malina@gmail.com' && 
         onlyUpdatingFields(['verificationStatus', 'isVerified', 'updatedAt']))
      );
      
      // Удаление: только владелец или администратор
      allow delete: if request.auth != null && (
        request.auth.uid == profileId ||
        request.auth.token.email == 'malina@gmail.com'
      );
    }
    
    // 🌍 ПРАВИЛА ДЛЯ ОДОБРЕННЫХ МЕСТ - ПУБЛИЧНЫЙ ДОСТУП
    match /approvedPlaces/{placeId} {
      // 🔓 ПУБЛИЧНОЕ ЧТЕНИЕ - ВСЕ МОГУТ ЧИТАТЬ (включая незалогиненных)
      allow read: if true;
      
      // 🔒 Создание и обновление: только администраторы
      allow create, update: if request.auth != null && 
                           request.auth.token.email == 'malina@gmail.com';
      
      // 🔒 Удаление: только администраторы
      allow delete: if request.auth != null && 
                    request.auth.token.email == 'malina@gmail.com';
    }
    
    // Функция для проверки, что обновляются только определенные поля
    function onlyUpdatingFields(allowedFields) {
      return resource.data.diff(request.resource.data).affectedKeys()
        .hasOnly(allowedFields);
    }
  }
}
```

### Шаг 3: Опубликуйте правила

1. Нажмите кнопку **Опубликовать** (Publish)
2. Подтвердите изменения

## 🚀 КРИТИЧНО: Обязательно обновите правила в Firebase Console!

⚠️ **Локальный файл правил не применяется автоматически!**

Правила работают только после ручного обновления в Firebase Console.

## 🔍 Что делают обновленные правила:

- ✅ **Публичный доступ** - все могут читать одобренные места (даже без авторизации)
- ✅ **Пользователи** могут создавать и редактировать только свои данные
- ✅ **Бизнес-заявки** может создать только владелец
- ✅ **Администратор** (`malina@gmail.com`) может одобрять/отклонять заявки
- ✅ **Безопасность** - только администраторы могут создавать/изменять места

## 🧪 После обновления правил:

1. **Перезагрузите приложение** полностью
2. **Проверьте консоль** - ошибка должна исчезнуть
3. **Незалогиненные пользователи** должны видеть места

## ⚠️ Важно:

- Правила применяются **немедленно** после публикации
- **Обязательно перезагрузите приложение** после изменения правил
- Убедитесь что правила точно скопированы в Firebase Console 