// Импортируем тип для конфигурации Next.js
import type { NextConfig } from "next";

// Определяем конфигурацию
const nextConfig: NextConfig = {
    // --- НАЧАЛО НАСТРОЕК ---

    // Пример настройки (можешь оставить или убрать, если не нужна)
    reactStrictMode: true,

    // --- СЕКЦИЯ images ---
    // Разрешаем загрузку изображений с img.freepik.com
    images: {
        remotePatterns: [
            {
                protocol: 'https', // Указываем протокол (обычно https)
                hostname: 'img.freepik.com', // Указываем хост, с которого разрешаем грузить картинки
                // port: '', // Можно указать порт, если он не стандартный (обычно не нужно)
                // pathname: '/free-photo/**', // Можно ограничить конкретный путь, если нужно разрешить только часть сайта
            },
            // Сюда можно добавить другие разрешенные домены, если понадобятся
            // {
            //   protocol: 'https',
            //   hostname: 'example.com',
            // },
        ],
    },
    // --- КОНЕЦ СЕКЦИИ images ---

    // --- Здесь могут быть другие твои настройки, если они есть ---

}; // Не забудь закрыть объект конфигурации

// Экспортируем конфигурацию для Next.js
export default nextConfig;